fastapi
uvicorn

httpx>=0.24.0

langchain
langgraph
langchain-groq
langchain-community

pymongo
python-dotenv

tiktoken

python-docx
pypdf

sse-starlette

langsmith

exa-py
langchain-mcp-adapters

# NLP (keep but lazy load)
spacy

httpx>=0.24.0
# embeddings are provided by a separate embedding service; do not install heavy libs here
# sentence-transformers
# torch (installed only in embedding service)